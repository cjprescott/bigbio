import React from 'react'
import ReactDOM from 'react-dom/client'
import BigBio from './BigBioApp'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <BigBio />
  </React.StrictMode>,
)
